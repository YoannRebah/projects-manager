import { TitlePopinDirective } from './title-popin.directive';

describe('TitlePopinDirective', () => {
  it('should create an instance', () => {
    const directive = new TitlePopinDirective();
    expect(directive).toBeTruthy();
  });
});
