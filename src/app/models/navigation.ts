export interface Navigation {
    href: string,
    iconClassNames?: string | null,
    html?: string | null,
    target?: string | null,
    ariaLabel: string,
    classNames?: string | null,
    dataTitlePopin?: string | null,
}